/**
 * テスト対象: 管理ダッシュボードのグラフコンポーネント（Issue #271）
 * 戦略: vi.mock('../api/client') で時系列集計 API をモックし、
 *   - 折れ線グラフ（投稿数・アクティブユーザー）の描画
 *   - チャンネル別投稿ボリュームのスタック/グルーピング表示
 *   - Top N チャンネル横棒グラフの描画
 *   - 期間フィルタ（PeriodFilterBar）と連動して時系列 API が再呼び出しされること
 *   - データ空・ローディング・エラー時のフォールバック表示
 * を検証する。アサーションは別 PR で実装する。
 *
 * 注意: Recharts はライブラリ自身の SVG 描画を testing-library で深く検証しないこと。
 * data-testid を持つコンテナの存在、API 呼び出し回数・引数、フォールバック表示の有無を主に確認する。
 */

describe('AdminCharts: 管理ダッシュボードのグラフ表示（Issue #271）', () => {
  describe('時系列グラフの描画', () => {
    it.todo('期間 7d で投稿数の折れ線グラフが描画される');
    it.todo('期間 7d でアクティブユーザー数の折れ線グラフが描画される');
    it.todo('時系列データが取得できたらローディングが消えてグラフが表示される');
    it.todo('時系列データが空配列の場合は「データがありません」のフォールバックを表示する');
    it.todo('時系列 API がエラーを返した場合は ErrorBoundary またはエラーメッセージを表示する');
  });

  describe('期間フィルタとの連動', () => {
    it.todo('PeriodFilterBar で 24h を選択すると時系列 API が period=24h で再呼び出しされる');
    it.todo('PeriodFilterBar で 30d を選択すると時系列 API が period=30d で再呼び出しされる');
    it.todo('カスタム期間（from/to）を適用すると時系列 API が from/to パラメータで呼び出される');
    it.todo('期間フィルタを変更すると Top チャンネル API も同じ期間で再呼び出しされる');
    it.todo('初回マウント時は URL パラメータの期間（または既定の 7d）で API が呼び出される');
  });

  describe('チャンネル別投稿ボリューム', () => {
    it.todo('チャンネル別の時系列を取得して同一グラフ上に複数系列で表示する');
    it.todo('チャンネル数が多い場合でも凡例（legend）が表示される');
    it.todo('対象チャンネルが 0 件の場合は空状態フォールバックを表示する');
  });

  describe('チャンネル別 Top N 横棒グラフ', () => {
    it.todo('上位 N チャンネルの投稿数が降順で横棒グラフに描画される');
    it.todo('チャンネル名と件数が読み取り可能な形でラベル表示される');
    it.todo('Top N の件数（N=10 など）が API 呼び出しの limit と一致する');
    it.todo('期間内に投稿が無い場合は空状態フォールバックを表示する');
  });

  describe('レイアウト・アクセシビリティ', () => {
    it.todo('各グラフカードに見出し（タイトル）が付与される');
    it.todo('統計タブ内に既存の数値カードと並んでグラフセクションが表示される');
    it.todo('期間フィルタは統計タブ全体（数値カード・グラフ）で共有される');
  });

  describe('Promise の安定化（React 19）', () => {
    it.todo('期間フィルタが変わらない限り時系列 API は再呼び出しされない（useMemo で安定化）');
    it.todo('タブ切替で再マウントされても同じ期間なら追加の API 呼び出しは発生しない');
  });
});
