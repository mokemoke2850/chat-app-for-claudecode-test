/**
 * テスト対象: components/Chat/TemplatePicker.tsx
 * 戦略:
 *   - ネットワーク通信は vi.mock('../api/client') で差し替える
 *   - テンプレート一覧の表示・絞り込み・キーボード操作・選択後コールバックを検証する
 *   - jsdom で動作しない部分はスタブで代替する
 */

import { describe, it, vi } from 'vitest';

vi.mock('../api/client', () => ({
  api: {
    templates: {
      list: vi.fn(),
    },
  },
}));

describe('TemplatePicker', () => {
  describe('テンプレート一覧表示', () => {
    it('テンプレート一覧がタイトルと本文プレビューで表示される', () => {
      // TODO
    });

    it('テンプレートが存在しない場合に空の旨のメッセージが表示される', () => {
      // TODO
    });
  });

  describe('テキスト絞り込み', () => {
    it('検索ボックスに入力するとタイトルに前方一致するテンプレートのみ表示される', () => {
      // TODO
    });

    it('検索テキストをクリアすると全テンプレートが再表示される', () => {
      // TODO
    });

    it('どのテンプレートにも一致しない場合に「該当なし」の旨が表示される', () => {
      // TODO
    });
  });

  describe('キーボード操作', () => {
    it('↓キーで次の候補にフォーカスが移る', () => {
      // TODO
    });

    it('↑キーで前の候補にフォーカスが移る', () => {
      // TODO
    });

    it('Enterキーでフォーカス中のテンプレートが選択される', () => {
      // TODO
    });

    it('Escapeキーでピッカーが閉じる', () => {
      // TODO
    });
  });

  describe('テンプレート選択', () => {
    it('テンプレートをクリックすると onSelect コールバックが本文を引数で呼ばれる', () => {
      // TODO
    });

    it('onSelect 呼び出し後にピッカーが閉じる', () => {
      // TODO
    });
  });
});
